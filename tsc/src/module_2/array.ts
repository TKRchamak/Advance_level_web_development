const nameList: string[] = ['suput', 'tupu', 'luchu', 'ghochu'];

const todoList: (string | number | boolean)[] = ['name', 20, true];

const userTuple: [string, number] = ['tonmoy', 209]; // tuple in ts is list of two properties, as like as pair
